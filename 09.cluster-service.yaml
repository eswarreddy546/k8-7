apiVersion: v1
kind: Service
metadata:
  name: service-demo
  namespace: roboshop

spec:
  selector:
    project: dev-roboshop
    env: dev

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80