apiVersion: v1
kind: Service
metadata:
  name: service-demo

spec:
  containers:
    - name: nginx
      image: nginx

      ports:
        port: 80
    
  envfrom:
   - ConfigMapRef:
      - name: demo-service